#include "brute_force.h"
#include <iostream>

int main()
{
    // Generate list of random coordinates (grouped and labeled)

    // Show the groups to the user (saying how many coords are in each group)

    // Get user input: which groups to visit and in which order

    // Determine distances between all points from one group to the next
    // this latter part is specified by the user

    // Compute path
    // FOR BRUTE FORCE ALGORITHM:
    // Compute all possible paths, keeping track of the 5 longest and shortest
    // and the route taken for each
    brute_force();
    // Display the shortest and longest paths to the user
}
